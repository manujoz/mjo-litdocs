<tbody class="table-body">
    <slot />
</tbody>

<style>
    .table-body {
        padding-top: 10px;
    }
    .table-body :global(td) {
        text-align: left;
        padding: 0.5rem 0.75rem;
        font-weight: 500;
        color: var(--mjo-foreground-color);
    }
    .table-body :global(tr) :global(td) {
        border-top: solid 1px var(--mjo-border-color);
    }
    .table-body :global(tr:first-child) :global(td) {
        padding-top: 1rem;
        border-top: none;
    }
</style>
