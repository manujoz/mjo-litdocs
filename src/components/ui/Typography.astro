---
export interface Props {
    size?: "xs" | "sm" | "md" | "lg" | "xl" | "title";
    weight?: "light" | "normal" | "bold";
    color?: "primary" | "secondary" | "accent" | "default";
    tag?: "p" | "span" | "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
    class?: string;
}

const { size = "md", weight = "normal", color = "default", tag = "span", class: className } = Astro.props;

const Element = tag;
---

<Element class:list={["container", "text-" + color, "font-" + weight, "text-" + size, className]} {...Astro.props}>
    <slot />
</Element>

<style>
    .container {
        line-height: 1.5em;
        margin: 0;
        padding: 0;
    }
    .text-primary {
        color: var(--mjo-primary-color);
    }
    .text-secondary {
        color: var(--mjo-secondary-color);
    }
    .text-accent {
        color: var(--mjo-secondary-color);
    }
    .font-light {
        font-weight: 300;
    }
    .font-normal {
        font-weight: 400;
    }
    .font-bold {
        font-weight: 700;
    }
    .text-xs {
        font-size: var(--mjo-font-size-xsmall);
    }
    .text-sm {
        font-size: var(--mjo-font-size-small);
    }
    .text-lg {
        font-size: var(--mjo-font-size-large);
    }
    .text-xl {
        font-size: var(--mjo-font-size-xlarge);
    }
    .text-title {
        font-size: var(--mjo-font-size-title1);
        line-height: 1em;
    }
</style>
