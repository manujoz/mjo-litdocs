<!-- Component Preview Placeholder -->
<div class="component-preview">
    <div class="preview-window">
        <div class="preview-header">
            <div class="preview-controls">
                <div class="control-dot control-red"></div>
                <div class="control-dot control-yellow"></div>
                <div class="control-dot control-green"></div>
            </div>
            <div class="preview-title">Component Preview</div>
        </div>
        <div class="preview-content">
            <div class="preview-component">
                <!-- This will be replaced with actual Lit components -->
                <div class="demo-button">Click me</div>
                <div class="demo-card">
                    <div class="demo-card-header">Card Title</div>
                    <div class="demo-card-content">Beautiful card component with smooth animations.</div>
                </div>
            </div>
        </div>
    </div>
</div>
<style>
    .component-preview {
        position: relative;
        margin-left: auto;
        margin-right: auto;
        max-width: 32rem;
    }

    .preview-window {
        border-radius: 0.5rem;
        border: 1px solid var(--color-border);
        background-color: oklch(94% 0.07 85);
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        overflow: hidden;
    }

    .preview-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding-left: 1rem;
        padding-right: 1rem;
        padding-top: 0.75rem;
        padding-bottom: 0.75rem;
        background-color: color-mix(in oklch, var(--color-muted) 50%, transparent);
        border-bottom: 1px solid var(--color-border);
    }

    .preview-controls {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .control-dot {
        width: 0.75rem;
        height: 0.75rem;
        border-radius: 50%;
    }

    .control-red {
        background-color: oklch(62% 0.257 29);
    }

    .control-yellow {
        background-color: oklch(84% 0.167 83);
    }

    .control-green {
        background-color: oklch(70% 0.177 135);
    }

    .preview-title {
        font-size: 0.875rem;
        font-weight: 500;
        color: var(--color-muted-foreground);
    }

    .preview-content {
        padding: 1.5rem;
    }

    .preview-component {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .demo-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding-left: 1rem;
        padding-right: 1rem;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
        background-color: var(--color-primary);
        color: var(--color-primary-foreground);
        font-weight: 500;
        border-radius: 0.375rem;
        transition: background-color 0.15s ease-in-out;
        cursor: pointer;
        border: none;
    }

    .demo-button:hover {
        background-color: color-mix(in oklch, var(--color-primary) 90%, transparent);
    }

    .demo-card {
        border-radius: 0.5rem;
        border: 1px solid var(--mjo-border-color);
        background-color: oklch(96% 0.04 85);
        padding: 1rem; /* p-4 */
        box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    }

    .demo-card-header {
        font-weight: 600;
        color: var(--color-foreground);
        margin-bottom: 0.5rem;
    }

    .demo-card-content {
        font-size: 0.875rem;
        color: var(--color-muted-foreground);
    }
</style>
